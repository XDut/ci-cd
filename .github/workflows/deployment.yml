name: Deploy to Render

on:
  push:
    branches:
      - [cite_start]main  # or feat/video-cicd-ecs as per your backend config 

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deploy
        run: |
          curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK_URL }}