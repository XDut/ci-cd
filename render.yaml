services:
  - type: web
    name: webapp-cicd
    runtime: docker
    plan: free
    buildCommand: yarn install --frozen-lockfile
    startCommand: node app.js
    envVars:
      - key: NODE_ENV
        value: production
